{% extends 'base.html.twig' %}

{% block body %}
<div>
    {{ form_start(commentForm) }}
    {{ form_widget(commentForm) }}
    <button>Update commentary</button>
    {{ form_end(commentForm) }}
</div>
{% endblock %}

{% block title %}

{% endblock %}

